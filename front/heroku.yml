build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
    API_URL: https://uokun-service-api-staging.herokuapp.com
    # 例) API_URL: https://my-app.herokuapp.com
run:
  web: yarn run start
